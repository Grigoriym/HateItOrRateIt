name: Release Prepare

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version name (e.g. 1.6.0)'
        required: true
      version_code:
        description: 'Version code (e.g. 31)'
        required: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Check out master
        uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Merge dev into master locally
        run: |
          git fetch origin dev
          git merge origin/dev --no-ff -m "Merge dev into master for release v${{ inputs.version }}"

      - name: Create release branch
        run: git checkout -b release/v${{ inputs.version }}

      - name: Bump version in app/build.gradle.kts
        run: |
          sed -i 's/versionCode = .*/versionCode = ${{ inputs.version_code }}/' app/build.gradle.kts
          sed -i 's/versionName = ".*"/versionName = "${{ inputs.version }}"/' app/build.gradle.kts

      - name: Create F-Droid changelog stub
        run: |
          echo "What's new in v${{ inputs.version }}" > fastlane/metadata/android/en-US/changelogs/${{ inputs.version_code }}.txt

      - name: Create Play Store changelog stub
        run: |
          mkdir -p playstore/changelogs
          echo "What's new in v${{ inputs.version }}" > playstore/changelogs/${{ inputs.version_code }}.txt

      - name: Commit and push
        run: |
          git add app/build.gradle.kts fastlane/metadata/android/en-US/changelogs/ playstore/changelogs/
          git commit -m "Release v${{ inputs.version }}"
          git push origin release/v${{ inputs.version }}

      - name: Create PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --base master \
            --head release/v${{ inputs.version }} \
            --title "Release v${{ inputs.version }}" \
            --body "## Release v${{ inputs.version }}

          **Version code:** ${{ inputs.version_code }}

          ### Before merging
          - [ ] Edit F-Droid changelog: \`fastlane/metadata/android/en-US/changelogs/${{ inputs.version_code }}.txt\`
          - [ ] Edit Play Store changelog: \`playstore/changelogs/${{ inputs.version_code }}.txt\` (copy-paste into Play Console manually — not automated)
          - [ ] Review version bump in \`app/build.gradle.kts\`

          ### After merging (automatic)
          - Tag \`v${{ inputs.version }}\` will be created and pushed
          - Existing \`release.yml\` will build APKs/AAB and create the GitHub Release
          - \`master\` will be back-merged into \`dev\`"
